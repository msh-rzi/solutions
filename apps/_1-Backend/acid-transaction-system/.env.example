# Local environment template for acid-transaction-system.
# Copy this file to `.env` in this app directory.

APP_HOST=localhost
PORT_ACID_TRANSACTION_SYSTEM=3020
# URL-encode special characters in passwords (e.g. @ => %40).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/acid-transaction-system
