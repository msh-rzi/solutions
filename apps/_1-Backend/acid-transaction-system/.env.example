# This app reads env vars from the workspace root `.env` by default.
# Required variables for acid-transaction-system:

APP_HOST=0.0.0.0
PORT_ACID_TRANSACTION_SYSTEM=3020
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/acid-transaction-system
